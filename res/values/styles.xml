<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 앱 전체에 적용할 머티리얼 테마 -->
    <style name="Theme.Exitsw" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- 커스텀 색상 또는 필요한 항목 추가 -->
        <item name="colorPrimary">@color/your_theme_color</item>
        <item name="colorPrimaryVariant">@color/your_theme_color</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/your_theme_color</item>
    </style>

    <!-- 기존 버튼 커스텀 스타일 유지 -->
    <style name="NoBackground_ImageButton">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
